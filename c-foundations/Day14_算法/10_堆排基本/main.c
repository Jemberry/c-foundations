#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

typedef struct node_s {
	int data;
	struct node_s *left;
	struct node_s *right;
}Node;

int main(void) {

	// 树：一对多的结构。 
	// 二叉树：  一个节点后可以跟两个结点。 
	// 堆： 就是一个二叉树。 它可以用数组表示。 

	return 0;
}